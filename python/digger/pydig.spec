# -*- mode: python ; coding: utf-8 -*-


a = Analysis(
    ['pydig.py'],
    pathex=[],
    binaries=[],
    datas=[('C:/Users/showr/AppData/Roaming/Python/Python312/site-packages/whois/data/public_suffix_list.dat', './whois/data/')],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='pydig',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
